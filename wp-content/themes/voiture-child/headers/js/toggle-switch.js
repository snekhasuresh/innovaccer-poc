// document.addEventListener("DOMContentLoaded", function () {
//   const toggle = document.querySelector(".custom_toggle_switch");
    
//   if (toggle) {
//     toggle.addEventListener("change", function () {
//       // Check if the toggle is checked
//       let value = this.checked ? "motorcycle" : "car";
//       // Set the cookie with the selected value (motorcycle or car)
//       document.cookie = "vehicleType=" + value + "; path=/";
//       // Reload the page to apply the change
//       location.reload();
//     });
//   }
// });
